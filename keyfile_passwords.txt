1) ENCRYPT THE PRIVATE KEY

openssl pkey \
  -in node1.key \
  -aes256 \
  -out node1-encrypted.key

:-

Enter PEM pass phrase:
Verifying - Enter PEM pass phrase:



2) USE THE ENCRYPTED KEY WHEN CREATING THE P12:

openssl pkcs12 -export \
  -in node1.crt \
  -inkey node1-encrypted.key \
  -certfile new-ca.crt \
  -out node1.p12 \
  -name node1

You' will be asked for the P12 export password.

So you deliberately have two passwords, matching the architecture you're describing:

Password	      Protects
PEM passphrase	node1-encrypted.key
P12 password	  node1.p12

OTHERWSIE:-

openssl pkcs12 -export \
  -out transport.p12 \
  -inkey server.key \
  -in server.crt \
  -certfile ca.crt \
  -name transport

================





But there's an important Elasticsearch issue

Before you proceed, we should establish exactly how the existing cluster is using that PEM password. Elasticsearch normally works with the private key inside the PKCS#12 keystore; it doesn't normally need you to separately configure a PEM private-key password when the key is packaged into PKCS#12.

If your existing setup genuinely has both passwords configured in Elasticsearch, I don't want to tell you to recreate it based on assumptions.

If you can provide the relevant existing elasticsearch.yml SSL settings and the output of:

bin/elasticsearch-keystore list

(without showing any password values), I can map exactly which password is being used where and tell you how to reproduce it on the new cluster.

You can upload the config if that's easier:

Upload a file
